import React from 'react'
import './PortalPager.css';

const PortalPager = (props) => {
    const { curr } = props;
    const { prev } = props;
    const { next } = props;
    
    return (

        <Container className="c-pager">
        </Container>
    );
}

export default PortalPager;